name: Deploy to GitHub Pages

on:
  push:
    branches:
      - main

# Sets permissions of the GITHUB_TOKEN to allow deployment to GitHub Pages
permissions:
  contents: write
  pages: write
  id-token: write

# Allow only one concurrent deployment
concurrency:
  group: "pages"
  cancel-in-progress: true

env:
  NODE_ENV: production
  CI: false

jobs:
  build-and-deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'
          cache: 'npm'

      - name: Clean install dependencies
        run: |
          rm -rf node_modules package-lock.json
          npm install

      - name: Install Parcel dependencies
        run: |
          npm install -g parcel@2.14.1
          npm install --save-dev @parcel/config-default@2.14.1 @parcel/core@2.14.1 @parcel/packager-css@2.14.1 @parcel/packager-html@2.14.1 @parcel/packager-js@2.14.1 @parcel/reporter-cli@2.14.1 @parcel/resolver-default@2.14.1 @parcel/runtime-browser-hmr@2.14.1 @parcel/runtime-js@2.14.1 @parcel/transformer-css@2.14.1 @parcel/transformer-html@2.14.1 @parcel/transformer-js@2.14.1 @parcel/transformer-raw@2.14.1 @parcel/optimizer-cssnano@2.14.1 @parcel/optimizer-terser@2.14.1 @parcel/optimizer-htmlnano@2.14.1

      - name: Build
        run: npm run build

      - name: Setup Pages
        uses: actions/configure-pages@v4

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: './dist'

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }} 